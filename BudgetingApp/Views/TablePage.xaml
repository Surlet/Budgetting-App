<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="BudgetingApp.Views.TablePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:BudgetingApp.ViewModels"
    Title="TablePage">
    <VerticalStackLayout>
        <CollectionView ItemsSource="{Binding ExpensesCollection}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BorderColor="Black" CornerRadius="20">
                        <Grid
                            Padding="0"
                            ColumnDefinitions="*,*,*,*,*">
                            <Label Text="{Binding Title}" />
                            <Label Text="{Binding StoreName}" Grid.Column="1"/>
                            <Label
                                Grid.Column="2"
                                Text="{Binding Category}"
                                VerticalOptions="End" />
                            <Label Grid.Column="3" Text="{Binding Amount, StringFormat='{0:C}'}" />
                            <Label Text="{Binding CreatedTime}" Grid.Column="4" />
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>